import { addons } from '@storybook/manager-api';

addons.setConfig({
    sidebar: {
        showRoots: true,
    },
});
// module.exports = {
//     alias: { prettier$: require.resolve('prettier/standalone') },
// };
