import {
    Fragment,
} from "react";
import {
    FigureProps,
} from "../@types";

export const Figure = (props: FigureProps) => {
    return (
        <Fragment>
            <figure>

            </figure>
        </Fragment>
    );
};
